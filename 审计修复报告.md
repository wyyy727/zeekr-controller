# 审计报告缺陷修复完成报告

**提交**：`af4c4c0` → 已推送 `main`（CNB: `nanamikiki/zeekr-controller`）
**验证**：`121 passed`（新增 22 个测试）· CNB 流水线 `cnb-4cr-1k2p2n08e` **success**
**改动**：10 个文件，+799 / −70

---

## 修复清单

| 优先级 | 缺陷 | 文件 | 状态 |
|---|---|---|---|
| P0-1 | 未验证指令未拦截（生产路径 0 调用） | `adapters/live_client.py` | ✅ |
| P0-2 | 账单去重误伤真实交易 | `bills/aggregator.py` | ✅ |
| P1-1 | 行程 `trip_id` 空串导致主键覆盖 | `adapters/live_client.py`、`core/store.py` | ✅ |
| P1-2 | 快照表随轮询无限增长 | `core/store.py` | ✅ |
| P2-1 | 行程缓存永不回源 | `api/trips.py` | ✅ |
| P2-2 | 统计行判定误杀真实交易 | `bills/parsers.py` | ✅ |
| P2-3 | 错误回显不一致且可能泄露令牌 | `api/__init__.py` 等 4 处 | ✅ |

---

## 关键实测数据

### P0-1 未验证指令拦截
`is_verified_command` 此前仅被**测试**引用，`send_command` 从未调用。现已接入闸门，
并补「接线测试」——用 spy 断言 `send_command` 内部真的调用了它。
> 只测零件（函数返回值）测不出这类脱线，必须测接线。

### P0-2 账单去重
改为「**订单号优先，无订单号才退化为时间指纹**」，两者**互斥而非叠加**。
叠加时早先版本会让「同分钟同额的两笔真实消费」被时间指纹抢先判重。

### P1-1 行程覆盖
```
旧逻辑 写入3条 -> 1 条    （trip_id 全为空串，INSERT OR REPLACE 互相覆盖）
新逻辑 写入3条 -> 3 条    （derived-5ee9984b… / derived-cdec0c81… / derived-ed795168…）
```
新增 `build_trip_id()` 派生稳定指纹 + store 层空值防御（宁可少写一条，也不覆盖一条）。

### P1-2 快照膨胀
```
轮询次数:            17280 / 天
旧行为(每条都写):     17280 行/天
新行为(节流后):         144 行/天      ← 120× 降幅，且每行都是真实状态变化
```
顺手修掉一个隐藏地雷：`created_at` 原用 SQLite `CURRENT_TIMESTAMP`（**UTC**），
与本地 `datetime.now()` 差 8 小时，会让节流比较**永远判定间隔足够**而彻底失效。

### P2-2 统计行误杀
`"共" in row and "笔" in row` → 改为 `共\s*\d+\s*笔` 且**金额不可解析**才算统计行。
「共笔文具店」「合并开票 共2笔」这类真实交易不再被吞。

### P2-3 错误回显
`auth / charges / trips` 原为 `detail=f"…{exc}"` 回显原始异常（网关报文可能夹带
accessToken 片段），而 `vehicle` 已正确不回显——同类错误两套写法。现统一为
`upstream_error` 助手：**对外固定文案，完整异常只进日志**，并加了静态检查测试防止回归。

---

## 附带发现（超出审计报告）

1. **`Query(False)` 作为默认值是真值**
   `force: bool = Query(False)` 中 `Query(...)` 返回 FieldInfo 对象，直接调用函数时
   为真，导致 `not force` 恒为假、缓存判定被短路。已改裸 `False`。
   这个 bug 是被我写的新测试抓出来的——如果没有「TTL 内不应回源」这条断言，它会静默溜过。

2. **`build_trip_id` 派生指纹含里程**
   同一秒同里程跑出两条行程的概率可忽略，而网关行程记录天然按时间区分，指纹足够唯一。

---

## 未做的事 / 已知权衡

- **P0-2 跨平台重复**：同一笔消费在支付宝与微信**各带不同交易单号**时会计为两笔。
  这是**有意为之**——多记一笔对账时能发现，少记一笔（静默吞掉）用户永远查不到。
  安全侧偏向「宁可重复」。
- **`charge_records.created_at`** 仍用 SQLite UTC 默认值。该字段不参与间隔比较，
  不构成功能缺陷，改它会动到既有语义，故未动。
- **CNB push 不自动触发**：配置按官方文档写（`main: push:`），触发记录仍为 0。
  已保留 `api_trigger` / `web_trigger` 作为手动通道（本次即用 `api_trigger` 触发）。
- **iOS 真机安装**：沙箱内 GitHub TLS 不通，需先把代码弄到 GitHub 才能触发
  `macos-26` 工作流做真实 SDK 编译。

---

## CNB 流水线日志摘录

```
Swift version 6.1.3
prepared 25 swift files
parse: OK
typecheck: OK
✅ Linux 类型检查通过 (25 个文件)
Finished, code: 0, duration: 23.5s
```

（日志中的 warning 全部位于 `ci/SwiftUIShim.swift` 桩文件内部，与被检查的项目代码无关。）
